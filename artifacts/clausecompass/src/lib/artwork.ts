/*
 * Colours used only inside the decorative pictures. The photographs (the
 * banner, the papers beside the boundary statement, the sheet beside the
 * choice) stay light paper in both colour schemes, so the theme tokens do not
 * apply on them: handwritten and typed notes are always ink, the route is
 * always terracotta. The drawn pen on the cards of the choice is otherwise in
 * theme colours; only its trim is fixed brass. The sticky note beside the
 * sign-in form is the same pale yellow paper in both schemes, with ink on it.
 */
export const INK = "#1F2A3A";
export const TERRACOTTA = "#A84524";
export const BRASS = "#B8923A";
export const STICKY_NOTE = "#F5E7B4";
