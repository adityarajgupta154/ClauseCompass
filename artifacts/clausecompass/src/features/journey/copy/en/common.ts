export const skipToContent = "Skip to content";
/** The one line shown while a screen's code is fetched (a moment, on a slow connection). */
  export const loadingScreen = "Opening the next screen…";
